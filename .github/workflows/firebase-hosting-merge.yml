# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18
        
    - name: Install Angular CLI
      run: npm install -g @angular/cli
    - run: "npm install firebase-admin --save"
    - name: Install dependencies
      run: npm install

    - name: Build
      run: bash -c "ng build --configuration production"
      
    
    
    - name: Deploy to Firebase
      uses: FirebaseExtended/action-hosting-deploy@v0
      with:
          repoToken: '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'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BIT_GPA_CALCULATER }}'
          channelId: live
          projectId: bit-gpacalculator
          
